# Salin file ini jadi .env (di folder backend yang sama).
# sqlx::query! menghubungi database SAAT cargo build — Postgres harus sudah jalan.
#
# Contoh (sesuaikan user, password, nama database):
DATABASE_URL=postgres://postgres:password_anda@127.0.0.1:5432/mbg
